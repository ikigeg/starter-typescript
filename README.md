# Starter Typescript - Simple

Just a quick project with most of the things I commonly use with new Typescript
projects.

## Instructions

Clone the repo, copy .env.template to .env then run:

```
npm install && npm run build
```
